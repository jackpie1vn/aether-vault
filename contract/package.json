{
  "name": "contract",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "prepare-deploy": "hardhat run scripts/prepare-deploy.ts --network zama",
    "prepare-deploy:sepolia": "hardhat run scripts/prepare-deploy.ts --network sepolia",
    "deploy": "hardhat run scripts/deploy.ts --network zama",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia",
    "deploy:local": "hardhat run scripts/deploy.ts --network hardhat",
    "test": "hardhat test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@fhevm/hardhat-plugin": "^0.3.0-4",
    "@fhevm/solidity": "0.9.1",
    "@nomicfoundation/hardhat-ethers": "^3.1.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.6.1",
    "hardhat": "^2.26.3"
  }
}
